// Vendor entry point for emailjs-mime-parser (MIME message/multipart parsing).
// Rollup bundles this into vendor/mime-parser.js as an ESM module.
export { default as parseMime } from 'emailjs-mime-parser';
